# Django core
django>=5.0,<7.0
djangorestframework>=3.15
djangorestframework-simplejwt>=5.3

# Real-time / WebSockets
channels>=4.0
channels-redis>=4.2
daphne>=4.1

# Database
psycopg2-binary>=2.9
dj-database-url>=2.1

# CORS
django-cors-headers>=4.3

# Static files (production)
whitenoise[brotli]>=6.5

# LiveKit (video/audio calls)
livekit-api>=0.6

# Task queue
celery>=5.4
redis>=5.0

# Utilities
python-dotenv>=1.0
Pillow>=10.0
gunicorn>=21.0
requests>=2.31
boto3>=1.35,<2

# Push (FCM v1 data messages for WhatsApp-style background delivery)
# Pinned to 6.x: firebase-admin 7.x requires PyJWT>=2.10.1, which conflicts
# with djangorestframework-simplejwt (<2.10.0).
firebase-admin>=6.5,<7
