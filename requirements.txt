# Django core
-c requirements.lock
django==6.1.1
djangorestframework==3.18.1
djangorestframework-simplejwt>=5.3

# Real-time / WebSockets
channels>=4.0
channels-redis>=4.2
daphne>=4.1

# Database
psycopg2-binary>=2.9
dj-database-url>=2.1

# CORS
django-cors-headers>=4.3

# Static files (production)
whitenoise[brotli]>=6.5

# LiveKit (video/audio calls)
livekit-api>=0.6

# Task queue
celery>=5.4
redis>=5.0

# Utilities
python-dotenv>=1.0
Pillow>=10.0
gunicorn>=21.0
requests>=2.31
google-auth[requests]>=2.0,<3
cachecontrol>=0.14,<1
boto3>=1.35,<2

# Push (FCM v1 data messages for WhatsApp-style background delivery)
# Keep the existing Firebase major version until a separately tested upgrade.
firebase-admin>=6.5,<7
